import { useState } from 'react';
import './App.css';
import Node from './Node/Node';

function App() {
  const nodes = useState([])

  const buildGrid = () => {
    
  }

  return (
    <div className="App">
      <Node/>
    </div>
  );
}

export default App;
